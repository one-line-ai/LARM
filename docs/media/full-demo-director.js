const puppeteer=require('puppeteer-core');const fs=require('fs');const path=require('path');
const S=__dirname; const FPS=30; const DT=1000/FPS; const OUT=path.join(S,'frames_app');
const T=[]; const at=(s,fn)=>T.push([s,fn]);
(async()=>{
  fs.mkdirSync(OUT,{recursive:true});
  const browser=await puppeteer.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:'new',args:['--allow-file-access-from-files','--hide-scrollbars','--font-render-hinting=none']});
  const page=await browser.newPage(); await page.setViewport({width:1280,height:800,deviceScaleFactor:1});
  page.on('pageerror',e=>console.log('PAGEERR',e.message));
  await page.goto('file://'+path.join(S,'a/b/web/app.html'),{waitUntil:'load'});
  await new Promise(r=>setTimeout(r,1500));
  const ev=(fn,...a)=>page.evaluate(fn,...a);
  const cursorEl=sel=>ev(sel=>{const p=__elPos(sel);__cur.tx=p[0];__cur.ty=p[1];},sel);
  const clickNav=id=>ev(id=>{__cur.click=220;go(id);},id);
  const cap=t=>ev(t=>__caption(t),t);
  const hl=sel=>ev(sel=>hl(sel),sel);
  const scroll=(sel)=>ev(sel=>scrollTo2(sel),sel);
  at(0.0,()=>ev(()=>__card('AI 코딩 도구가 내 Mac에서','무엇까지 할 수 있는지 확인해 보신 적 있나요?',1)));
  at(3.4,()=>ev(()=>{document.getElementById('card').style.transition='opacity .6s';__card(null,null,0);}));
  at(3.8,()=>cap('LARM을 열면 지금 상태가 한눈에 들어옵니다. 캐릭터 라미의 표정만 봐도 알 수 있습니다.'));
  at(4.0,()=>cursorEl('#mascot'));
  at(8.2,()=>cap('위험한 설정을 찾아 이유와 고치는 방법까지 알려 줍니다.'));
  at(8.3,()=>cursorEl('#st-high')); at(9.4,()=>clickNav('findings')); at(9.6,()=>cursorEl('#fdetail'));
  at(13.2,()=>cap('게임이론 1, 권한 위임. 넓은 허용을 좁히면 확인 요청이 얼마나 늘지 실제 사용 기록으로 계산합니다.'));
  at(13.3,()=>scroll('#deleg')); at(13.4,()=>hl('#deleg')); at(13.5,()=>cursorEl('#deleg'));
  at(18.6,()=>cap('세션당 1.3회라면 좁혀도 부담이 없습니다. 귀찮음이 아니라 숫자로 결정합니다.'));
  at(22.8,()=>cap('예외를 되풀이하면 사실상 허용과 같다고 알려 줍니다. 약속을 지키게 돕는 장치입니다.'));
  at(22.9,()=>scroll('#exc')); at(23.0,()=>hl('#exc')); at(23.1,()=>cursorEl('#exc'));
  at(27.4,()=>cap('사용자와 AI 도구는 목표가 조금 다릅니다. LARM은 심판이 되어 둘의 선택을 바꿉니다.'));
  at(27.5,()=>hl(null)); at(27.6,()=>cursorEl('#nav-games')); at(28.6,()=>clickNav('games'));
  at(32.2,()=>cap('게임이론 2, 신호 읽기. 숨긴 명령이나 민감한 경로 접근 같은 행동을 점수로 만들어 먼저 살펴볼 세션을 알려 줍니다.'));
  at(32.3,()=>scroll('#g2')); at(32.4,()=>hl('#g2')); at(32.5,()=>cursorEl('#g2'));
  at(37.8,()=>cap('게임이론 3, 기억 있는 신뢰. 프로젝트마다 이력을 남겨 지난번에 위험했던 곳은 더 살펴봅니다.'));
  at(37.9,()=>scroll('#g3')); at(38.0,()=>hl('#g3')); at(38.1,()=>cursorEl('#g3'));
  at(43.0,()=>cap('게임이론 4, 예측할 수 없는 검사. 다음 점검 시각을 무작위로 정하니 그 사이에 몰래 바꿀 틈이 없습니다.'));
  at(43.1,()=>scroll('#g4')); at(43.2,()=>hl('#g4')); at(43.3,()=>cursorEl('#g4'));
  at(48.4,()=>cap('게임이론 5, 주의 배분. 조치가 필요할 확률과 사용자가 볼 확률을 곱해 가치가 높은 알림만 보냅니다. 늑대소년이 되지 않습니다.'));
  at(48.5,()=>scroll('#g5')); at(48.6,()=>hl('#g5')); at(48.7,()=>cursorEl('#g5'));
  at(54.2,()=>cap('모든 추정은 나중 결과와 비교해 얼마나 맞았는지 점수로 검증합니다.'));
  at(54.3,()=>scroll('#g7')); at(54.4,()=>hl('#g7')); at(54.5,()=>cursorEl('#g7'));
  at(58.4,()=>cap('Claude Code가 무엇을 하려 했는지 기록합니다. 내용은 저장하지 않고 종류와 표시만 남깁니다.'));
  at(58.5,()=>hl(null)); at(58.6,()=>cursorEl('#nav-activity')); at(59.6,()=>clickNav('activity')); at(60.0,()=>cursorEl('#sessbox'));
  at(63.6,()=>cap('기준 상태를 정해 두면 그 뒤 무엇이 바뀌었는지 바로 비교합니다.'));
  at(63.7,()=>cursorEl('#nav-changes')); at(64.7,()=>clickNav('changes'));
  at(68.4,()=>cap('설정 전체는 별자리처럼 탐색합니다. 별을 누르면 그곳으로 날아갑니다.'));
  at(68.5,()=>cursorEl('#nav-graph')); at(69.5,()=>clickNav('graph'));
  at(70.5,()=>cursorEl('#gal'));
  at(71.0,()=>page.frames().find(f=>f.url().includes('galaxy-embed')).evaluate(()=>{focusNode(G.idx.get('agent:claude-code'));}));
  at(74.6,()=>cap('점검 결과는 비밀값 없이 보고서로 묶어 전달하고, 받은 사람은 오프라인으로 검증합니다.'));
  at(74.7,()=>cursorEl('#nav-evidence')); at(75.7,()=>clickNav('evidence')); at(76.0,()=>cursorEl('#export'));
  at(80.0,()=>ev(()=>{__caption('');document.getElementById('card').style.transition='opacity .8s';__card('LARM','막지 않고, 보여 줍니다. 무료입니다.  github.com/one-line-ai/LARM',1);}));
  const END=84.0; const N=Math.round(END*FPS);
  T.sort((a,b)=>a[0]-b[0]); let ti=0;
  for(let f=0;f<N;f++){const t=f/FPS; while(ti<T.length&&T[ti][0]<=t){await T[ti][1]();ti++;}
    await ev(dt=>__step(dt),DT);
    await page.screenshot({path:path.join(OUT,`f${String(f).padStart(5,'0')}.jpg`),type:'jpeg',quality:92});
    if(f%300===0)console.log('frame',f,'/',N);}
  await browser.close(); console.log('DONE',N);
})().catch(e=>{console.error(e);process.exit(1);});
